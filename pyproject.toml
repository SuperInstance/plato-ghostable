[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-ghostable"
version = "0.1.0"
description = "Ghostable lifecycle — mark tiles/agents for active/ghost/resurrected states"
license = {text = "MIT"}
requires-python = ">=3.8"
